image: "rust:latest"

cache:
  paths:
    - target/
    - public/
    - .cargo

before_script:
  - export CARGO_HOME=.cargo
    
stages:
  - build
  - test
  - doc
  - deploy

cargo:build:
  stage: build
  script:
  - rustc --version && cargo --version      # Print version info for debugging
  - cargo build


cargo:test:
  stage: test
  script:
  - cargo test --all

cargo:doc:
  stage: doc
  script:
  - cargo doc
  - cp -r target/doc public/doc

cargo:mdbook:
  stage: doc
  script:
  - cargo install mdbook
  - mdbook build book --d public/book 
  
pages:
  stage: deploy
  script:
  - echo publishing pages
  artifacts:
    paths:
      - public
  only:
    - master
